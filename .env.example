# Clave privada de la wallet de deploy (SIN el prefijo 0x).
# NUNCA uses una wallet con fondos reales para esto.
DEPLOYER_PRIVATE_KEY=

# RPC endpoint de Sepolia (Alchemy, Infura, u otro proveedor).
SEPOLIA_RPC_URL=
